rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Audio files are publicly readable
    match /ielts-audio/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
